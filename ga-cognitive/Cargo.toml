[package]
name = "ga-cognitive"
version = "0.1.0"
edition = "2021"
description = "Cognitive systems framework for GA-Bagua semantic knowledge graphs"

[dependencies]
ga-semantics-core = { path = "../ga-semantics-core", features = ["store"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
