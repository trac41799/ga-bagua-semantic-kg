use crate::types::ConceptAnnotation;

pub fn smoke_test_annotations() -> Vec<ConceptAnnotation> {
    vec![
        ConceptAnnotation {
            concept_id: "rate_limiter".to_string(),
            name: "Rate Limiter".to_string(),
            description: "Restricts the number of requests a client can make in a time window".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("3.2 Traffic Control".to_string()),
            dominant_trigram: "Gen".to_string(),
            dominant_role: "constraining".to_string(),
            wuxing_phase: "Earth".to_string(),
            secondary_roles: vec!["balancing".to_string()],
            related_concepts: vec!["Circuit Breaker".to_string(), "Token Bucket".to_string()],
            relation_types: [
                ("Circuit Breaker".to_string(), "influential".to_string()),
                ("Token Bucket".to_string(), "generative".to_string()),
            ].into_iter().collect(),
            contradicts: vec!["Unrestricted Access".to_string()],
            suggested_coefficients: [0.04, -0.09, -0.51, 0.68, 0.21, -0.26, 0.17, -0.34],
        },
        ConceptAnnotation {
            concept_id: "circuit_breaker".to_string(),
            name: "Circuit Breaker".to_string(),
            description: "Prevents cascading failures by stopping calls to a failing service".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("3.3 Fault Tolerance".to_string()),
            dominant_trigram: "Gen".to_string(),
            dominant_role: "constraining".to_string(),
            wuxing_phase: "Earth".to_string(),
            secondary_roles: vec![],
            related_concepts: vec!["Rate Limiter".to_string(), "Fallback Handler".to_string()],
            relation_types: [
                ("Rate Limiter".to_string(), "clarifying".to_string()),
                ("Fallback Handler".to_string(), "generative".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.05, -0.05, -0.45, 0.70, 0.15, -0.20, 0.10, -0.30],
        },
        ConceptAnnotation {
            concept_id: "message_queue".to_string(),
            name: "Message Queue".to_string(),
            description: "Transmits events between services asynchronously with guaranteed delivery".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("4.1 Messaging".to_string()),
            dominant_trigram: "Kan".to_string(),
            dominant_role: "transmissive".to_string(),
            wuxing_phase: "Water".to_string(),
            secondary_roles: vec!["receptive".to_string(), "generative".to_string()],
            related_concepts: vec!["Event Bus".to_string(), "Message Broker".to_string()],
            relation_types: [
                ("Event Bus".to_string(), "receptive".to_string()),
                ("Message Broker".to_string(), "generative".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.15, 0.25, 0.81, -0.10, -0.15, 0.10, 0.30, 0.05],
        },
        ConceptAnnotation {
            concept_id: "authentication_service".to_string(),
            name: "Authentication Service".to_string(),
            description: "Verifies user identity and issues access tokens".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("2.1 Security".to_string()),
            dominant_trigram: "Li".to_string(),
            dominant_role: "clarifying".to_string(),
            wuxing_phase: "Fire".to_string(),
            secondary_roles: vec!["constraining".to_string()],
            related_concepts: vec!["Authorization Service".to_string()],
            relation_types: [
                ("Authorization Service".to_string(), "generative".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.05, -0.10, -0.10, 0.35, 0.75, -0.10, 0.15, -0.20],
        },
        ConceptAnnotation {
            concept_id: "load_balancer".to_string(),
            name: "Load Balancer".to_string(),
            description: "Distributes incoming traffic across multiple server instances".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("3.1 Distribution".to_string()),
            dominant_trigram: "Dui".to_string(),
            dominant_role: "balancing".to_string(),
            wuxing_phase: "Metal".to_string(),
            secondary_roles: vec!["transmissive".to_string()],
            related_concepts: vec!["Server Pool".to_string(), "Health Check".to_string()],
            relation_types: [
                ("Server Pool".to_string(), "transmissive".to_string()),
                ("Health Check".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.10, -0.15, 0.45, -0.20, 0.05, -0.10, 0.70, -0.05],
        },
        ConceptAnnotation {
            concept_id: "config_store".to_string(),
            name: "Config Store".to_string(),
            description: "Central repository for application configuration values with versioning".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("5.1 Configuration".to_string()),
            dominant_trigram: "Kun".to_string(),
            dominant_role: "receptive".to_string(),
            wuxing_phase: "Earth".to_string(),
            secondary_roles: vec![],
            related_concepts: vec!["Feature Flags".to_string()],
            relation_types: [
                ("Feature Flags".to_string(), "generative".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.72, 0.05, -0.10, 0.25, -0.05, 0.10, -0.15, 0.10],
        },
        ConceptAnnotation {
            concept_id: "event_trigger".to_string(),
            name: "Event Trigger".to_string(),
            description: "Initiates automated workflows when specific conditions are met".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("4.2 Automation".to_string()),
            dominant_trigram: "Zhen".to_string(),
            dominant_role: "causal".to_string(),
            wuxing_phase: "Wood".to_string(),
            secondary_roles: vec!["generative".to_string()],
            related_concepts: vec!["Workflow Engine".to_string(), "Event Bus".to_string()],
            relation_types: [
                ("Workflow Engine".to_string(), "generative".to_string()),
                ("Event Bus".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.10, 0.65, -0.15, -0.20, -0.10, 0.35, -0.15, 0.25],
        },
        ConceptAnnotation {
            concept_id: "monitoring_dashboard".to_string(),
            name: "Monitoring Dashboard".to_string(),
            description: "Visualizes system metrics and alerts with real-time updates".to_string(),
            document_id: "DOC-SMOKE".to_string(),
            section: Some("6.1 Observability".to_string()),
            dominant_trigram: "Xun".to_string(),
            dominant_role: "influential".to_string(),
            wuxing_phase: "Wood".to_string(),
            secondary_roles: vec!["clarifying".to_string()],
            related_concepts: vec!["Alert Manager".to_string()],
            relation_types: [
                ("Alert Manager".to_string(), "generative".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.05, 0.30, -0.10, -0.25, 0.20, 0.60, -0.15, 0.15],
        },
    ]
}

pub fn benchmark_50_annotations() -> Vec<ConceptAnnotation> {
    let mut concepts = smoke_test_annotations();

    concepts.extend(vec![
        ConceptAnnotation {
            concept_id: "token_bucket".to_string(),
            name: "Token Bucket".to_string(),
            description: "Algorithm that controls data transmission rate using tokens".to_string(),
            document_id: "DOC-01".to_string(),
            section: Some("3.2.1".to_string()),
            dominant_trigram: "Gen".to_string(),
            dominant_role: "constraining".to_string(),
            wuxing_phase: "Earth".to_string(),
            secondary_roles: vec!["transmissive".to_string()],
            related_concepts: vec!["Rate Limiter".to_string()],
            relation_types: [
                ("Rate Limiter".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.08, -0.12, -0.55, 0.62, 0.18, -0.22, 0.15, -0.28],
        },
        ConceptAnnotation {
            concept_id: "service_mesh".to_string(),
            name: "Service Mesh".to_string(),
            description: "Infrastructure layer for service-to-service communication with observability".to_string(),
            document_id: "DOC-01".to_string(),
            section: Some("7.1".to_string()),
            dominant_trigram: "Kan".to_string(),
            dominant_role: "transmissive".to_string(),
            wuxing_phase: "Water".to_string(),
            secondary_roles: vec!["influential".to_string()],
            related_concepts: vec!["API Gateway".to_string(), "Sidecar Proxy".to_string()],
            relation_types: [
                ("API Gateway".to_string(), "constraining".to_string()),
                ("Sidecar Proxy".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.12, 0.10, 0.70, -0.15, -0.05, 0.25, -0.10, 0.20],
        },
        ConceptAnnotation {
            concept_id: "api_gateway".to_string(),
            name: "API Gateway".to_string(),
            description: "Single entry point for all client requests, handles routing and auth".to_string(),
            document_id: "DOC-01".to_string(),
            section: Some("2.2".to_string()),
            dominant_trigram: "Gen".to_string(),
            dominant_role: "constraining".to_string(),
            wuxing_phase: "Earth".to_string(),
            secondary_roles: vec!["transmissive".to_string(), "clarifying".to_string()],
            related_concepts: vec!["Service Mesh".to_string(), "Authentication Service".to_string()],
            relation_types: [
                ("Service Mesh".to_string(), "generative".to_string()),
                ("Authentication Service".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.10, -0.05, -0.40, 0.65, 0.25, -0.15, 0.10, -0.20],
        },
        ConceptAnnotation {
            concept_id: "database_replica".to_string(),
            name: "Database Replica".to_string(),
            description: "Read-only copy of primary database for scaling read operations".to_string(),
            document_id: "DOC-01".to_string(),
            section: Some("5.3".to_string()),
            dominant_trigram: "Kun".to_string(),
            dominant_role: "receptive".to_string(),
            wuxing_phase: "Earth".to_string(),
            secondary_roles: vec!["balancing".to_string()],
            related_concepts: vec!["Primary Database".to_string(), "Load Balancer".to_string()],
            relation_types: [
                ("Primary Database".to_string(), "generative".to_string()),
                ("Load Balancer".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.68, 0.05, 0.10, 0.20, -0.05, 0.15, -0.10, -0.05],
        },
        ConceptAnnotation {
            concept_id: "caching_layer".to_string(),
            name: "Caching Layer".to_string(),
            description: "Stores frequently accessed data in memory to reduce database load".to_string(),
            document_id: "DOC-01".to_string(),
            section: Some("5.4".to_string()),
            dominant_trigram: "Li".to_string(),
            dominant_role: "clarifying".to_string(),
            wuxing_phase: "Fire".to_string(),
            secondary_roles: vec!["transmissive".to_string()],
            related_concepts: vec!["Database Replica".to_string(), "Config Store".to_string()],
            relation_types: [
                ("Database Replica".to_string(), "constraining".to_string()),
                ("Config Store".to_string(), "receptive".to_string()),
            ].into_iter().collect(),
            contradicts: vec![],
            suggested_coefficients: [0.05, -0.10, 0.20, -0.15, 0.70, -0.10, 0.15, -0.15],
        },
    ]);

    concepts
}

pub fn smoke_test_document() -> &'static str {
    include_str!("../data/documents/smoke_test_doc.md")
}
