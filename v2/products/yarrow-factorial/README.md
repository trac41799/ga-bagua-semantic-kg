# Yarrow — MOVED to its own project

**This in-repo entry is a pointer only. Yarrow has been extracted to its dedicated standalone project:**

**→ `D:\TRANSFER DATA\Coding\OpenCode\yarrow-factorial\`** (outside this repository)

## What's there (complete and verified)
- `yarrow/` package + `tests/` — **16/16 verification tests green** (the correctness gate)
- CLI (`yarrow design` / `yarrow decompose`) verified end-to-end
- `README.md`, `docs/PRODUCT_SPEC.md` (PRD), `docs/PRODUCT_PLAN.md` (phased plan), `docs/VALIDATION.md` (all banked evidence: 2³/2⁴ contrasts 22/22 exact, Möbius 3.91e-14, interaction recovery 2.2e-16), `docs/examples/` (generated reports), `LICENSE` (MIT), `.github/workflows/ci.yml` (correctness gate)

## Retained in-repo (historical)
- `PRODUCT_SPEC.md`, `PRODUCT_PLAN.md`, `REPO_HANDOFF.md` (extraction contract, marked DONE)

**Do not rebuild Yarrow code here — the dedicated project is the source of truth.**
