# POC-08 — Reframing Grammar v2 (few-shot naming)

**Status: MODEL_DEPENDENT; product PENDING.** The DeepSeek few-shot protocol passes its baseline: diversity Arm A 0.163 vs Arm B 0.453 (delta +0.290, bar +0.10), coherence 3.75 (bar 3.5). The gpt-4o-mini replication fails at diversity 0.296 and coherence 3.10, so the result is not model-general. The framework's naming layer works only with the validated model/protocol scope.

