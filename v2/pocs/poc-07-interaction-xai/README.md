# POC-07 — Feature-Interaction XAI

**Status: BUILT AND VALIDATED — math PASS, naming FAIL.** Interaction recovery exact (max planted err 2.2e-16; 3/3 planted subsets identified, no false positives). Bagua-named explanations rated +0.33 clearer (bar +0.5) — direction positive, margin missed. Math component ships (Yarrow); naming needs a stronger protocol under a new pre-registration.

