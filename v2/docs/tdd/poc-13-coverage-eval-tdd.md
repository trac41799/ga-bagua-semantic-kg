# TDD — POC-13 Coverage-Eval
**Cases:** E1: 24 outputs frozen (hash); bad outputs verifiably deficient (drop ≥2 roles — automated check). E2: rubric parse → 8 bits strictly; failure counted. E3: detection-rate math hand case. E4: runner renders rubric vs plain detection, specificity, verdicts (rubric ≥ plain +3 AND ≤2 false alarms). E5: cache determinism; tests green (≥9).
