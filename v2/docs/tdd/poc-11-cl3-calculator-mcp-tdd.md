# TDD — POC-11 Cl3 Calculator MCP
**Cases:** C1: 24 flip cases exact; 8 complements (Kan→Li etc.); 64 product-table entries vs verified PROD_TABLE; grades; combine. C2: unknown op → error; bad arg → error; multi-op list with a bad middle op → whole call errors (no partial). C3: 100 seeded random op sequences == reference POC-01 calculator results (1e-12). C4: MCP tools/list includes cl3_evaluate; tools/call returns exact JSON (sim). C5: official mcp.client.stdio client lists + calls the tool. C6: zero LLM imports in the tool path (import audit). **DoD:** pytest green (≥12), MCP-SDK test green, README with contract.
