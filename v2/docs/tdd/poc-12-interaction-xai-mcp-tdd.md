# TDD — POC-12 Interaction-Spectrum MCP
**Cases:** X1: exact recovery on the POC-07 planted function (max err ≤1e-9; 3/3 subsets; 0 FPs). X2: non-±1 points → error; length mismatch → error; empty → error. X3: MCP tools/list + tools/call (sim) returns spectrum dict. X4: official MCP client call. X5: no LLM imports; tests green (≥8).
