# TDD — POC-10 Communication Boundary-Test
**Cases:** 10.1 calibration sets: 6 pairs per task, hash-frozen, bad versions verifiably worse (drop ≥2 planted facts — automated check); 10.2 rater parse 1–5 strict; 10.3 QA questions: 3 per 06-transition, generated from planted deltas (aspect + values present); 10.4 reader scoring: exact contains-check hand case (both values → 1, one → 0); 10.5 09 answer-conveyance scoring hand case; 10.6 runner renders calibration + QA + rater verdicts; 10.7 SimulatedLLM runner exit 0; 10.8 determinism on cache. **DoD:** pytest green (≥10), real run verdicts, THESIS.md claim (d) updated with the result.
